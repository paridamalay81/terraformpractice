name: Github Code download
on:
  workflow_dispatch
jobs:  
  code-download:
    runs-on: ubuntu-latest
    steps:    
     - name: Download code
       uses: actions/checkout@v3
       with:
          ref: 'master'